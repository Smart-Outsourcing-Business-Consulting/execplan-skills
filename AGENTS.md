# Repository Instructions

## Skill Changes

When the user asks Codex to change a skill, make the change in this repository's
corresponding source skill file under `skills/` first.

Do not change installed Codex system skill files or installed `.agents` skill
files unless the user explicitly approves that destination for the current
task.
